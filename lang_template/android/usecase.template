package {{.PackageName}}.domain.repository

import {{.PackageName}}.domain.model.{{.Name}}Entity
import kotlinx.coroutines.flow.Flow

class {{.Name}}UseCase(
    private val repository: {{.Name}}Repository
) {
    operator suspend fun invoke() = repository.
}